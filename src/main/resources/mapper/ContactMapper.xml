<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhimai.xingyun.mapper.ContactMapper">

    <select id="findRecentContacts" resultType="com.zhimai.xingyun.domain.entity.Contact">
        SELECT 
            c.*
        FROM 
            contacts c
        JOIN (
            SELECT 
                contact_id, 
                MAX(interaction_time) as max_time
            FROM 
                interactions
            WHERE 
                interaction_time >= DATE_SUB(NOW(), INTERVAL 30 DAY)
            GROUP BY 
                contact_id
        ) i ON c.id = i.contact_id
        WHERE 
            c.user_id = #{userId}
        ORDER BY 
            i.max_time DESC
        LIMIT 10
    </select>

</mapper>
